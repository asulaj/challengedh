import { TimeSearchPipe } from './time-search.pipe';

describe('TimeSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
